//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "StaticJs.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
 #include "json/json.h" 
using namespace drogon;
std::string StaticJs::genText(const DrTemplateData& StaticJs_view_data)
{
	drogon::OStringStream StaticJs_tmp_stream;
	std::string layoutName{""};
 
    auto staticData = StaticJs_view_data.get<Json::Value>("staticData");
	StaticJs_tmp_stream << "var staticJson = ";
StaticJs_tmp_stream<<staticData.toStyledString();
	StaticJs_tmp_stream << ";\n";
if(layoutName.empty())
{
std::string ret{std::move(StaticJs_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = StaticJs_view_data;
auto str = std::move(StaticJs_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
